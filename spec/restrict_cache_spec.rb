require 'spec_helper'

describe RestrictCache do
  it 'has a version number' do
    expect(RestrictCache::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
